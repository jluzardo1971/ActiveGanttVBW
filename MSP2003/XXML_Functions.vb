Option Explicit On
Imports System.Xml

Partial Friend Class clsXML
	'// ----------------------------------------------------------------------------------------
	'// Microsoft Project 2003 XML Functions
	'// ----------------------------------------------------------------------------------------

	Friend Sub ReadProperty(ByVal sElementName As String, ByRef sElementValue As E_FYSTARTDATE)
		Dim yBuff As Short = 0
		yBuff = yReadProperty(sElementName, sElementValue)
		sElementValue = yBuff
	End Sub

	Friend Sub ReadProperty(ByVal sElementName As String, ByRef sElementValue As E_CURRENCYSYMBOLPOSITION)
		Dim yBuff As Short = 0
		yBuff = yReadProperty(sElementName, sElementValue)
		sElementValue = yBuff
	End Sub

	Friend Sub ReadProperty(ByVal sElementName As String, ByRef sElementValue As E_DEFAULTTASKTYPE)
		Dim yBuff As Short = 0
		yBuff = yReadProperty(sElementName, sElementValue)
		sElementValue = yBuff
	End Sub

	Friend Sub ReadProperty(ByVal sElementName As String, ByRef sElementValue As E_DEFAULTFIXEDCOSTACCRUAL)
		Dim yBuff As Short = 0
		yBuff = yReadProperty(sElementName, sElementValue)
		sElementValue = yBuff
	End Sub

	Friend Sub ReadProperty(ByVal sElementName As String, ByRef sElementValue As E_DURATIONFORMAT)
		Dim yBuff As Short = 0
		yBuff = yReadProperty(sElementName, sElementValue)
		sElementValue = yBuff
	End Sub

	Friend Sub ReadProperty(ByVal sElementName As String, ByRef sElementValue As E_WORKFORMAT)
		Dim yBuff As Short = 0
		yBuff = yReadProperty(sElementName, sElementValue)
		sElementValue = yBuff
	End Sub

	Friend Sub ReadProperty(ByVal sElementName As String, ByRef sElementValue As E_EARNEDVALUEMETHOD)
		Dim yBuff As Short = 0
		yBuff = yReadProperty(sElementName, sElementValue)
		sElementValue = yBuff
	End Sub

	Friend Sub ReadProperty(ByVal sElementName As String, ByRef sElementValue As E_WEEKSTARTDAY)
		Dim yBuff As Short = 0
		yBuff = yReadProperty(sElementName, sElementValue)
		sElementValue = yBuff
	End Sub

	Friend Sub ReadProperty(ByVal sElementName As String, ByRef sElementValue As E_BASELINEFOREARNEDVALUE)
		Dim yBuff As Short = 0
		yBuff = yReadProperty(sElementName, sElementValue)
		sElementValue = yBuff
	End Sub

	Friend Sub ReadProperty(ByVal sElementName As String, ByRef sElementValue As E_NEWTASKSTARTDATE)
		Dim yBuff As Short = 0
		yBuff = yReadProperty(sElementName, sElementValue)
		sElementValue = yBuff
	End Sub

	Friend Sub ReadProperty(ByVal sElementName As String, ByRef sElementValue As E_DEFAULTTASKEVMETHOD)
		Dim yBuff As Short = 0
		yBuff = yReadProperty(sElementName, sElementValue)
		sElementValue = yBuff
	End Sub

	Friend Sub ReadProperty(ByVal sElementName As String, ByRef sElementValue As E_TYPE)
		Dim yBuff As Short = 0
		yBuff = yReadProperty(sElementName, sElementValue)
		sElementValue = yBuff
	End Sub

	Friend Sub ReadProperty(ByVal sElementName As String, ByRef sElementValue As E_TYPE_1)
		Dim yBuff As Short = 0
		yBuff = yReadProperty(sElementName, sElementValue)
		sElementValue = yBuff
	End Sub

	Friend Sub ReadProperty(ByVal sElementName As String, ByRef sElementValue As E_ROLLUPTYPE)
		Dim yBuff As Short = 0
		yBuff = yReadProperty(sElementName, sElementValue)
		sElementValue = yBuff
	End Sub

	Friend Sub ReadProperty(ByVal sElementName As String, ByRef sElementValue As E_CALCULATIONTYPE)
		Dim yBuff As Short = 0
		yBuff = yReadProperty(sElementName, sElementValue)
		sElementValue = yBuff
	End Sub

	Friend Sub ReadProperty(ByVal sElementName As String, ByRef sElementValue As E_VALUELISTSORTORDER)
		Dim yBuff As Short = 0
		yBuff = yReadProperty(sElementName, sElementValue)
		sElementValue = yBuff
	End Sub

	Friend Sub ReadProperty(ByVal sElementName As String, ByRef sElementValue As E_DAYTYPE)
		Dim yBuff As Short = 0
		yBuff = yReadProperty(sElementName, sElementValue)
		sElementValue = yBuff
	End Sub

	Friend Sub ReadProperty(ByVal sElementName As String, ByRef sElementValue As E_TYPE_2)
		Dim yBuff As Short = 0
		yBuff = yReadProperty(sElementName, sElementValue)
		sElementValue = yBuff
	End Sub

	Friend Sub ReadProperty(ByVal sElementName As String, ByRef sElementValue As E_FIXEDCOSTACCRUAL)
		Dim yBuff As Short = 0
		yBuff = yReadProperty(sElementName, sElementValue)
		sElementValue = yBuff
	End Sub

	Friend Sub ReadProperty(ByVal sElementName As String, ByRef sElementValue As E_CONSTRAINTTYPE)
		Dim yBuff As Short = 0
		yBuff = yReadProperty(sElementName, sElementValue)
		sElementValue = yBuff
	End Sub

	Friend Sub ReadProperty(ByVal sElementName As String, ByRef sElementValue As E_LEVELINGDELAYFORMAT)
		Dim yBuff As Short = 0
		yBuff = yReadProperty(sElementName, sElementValue)
		sElementValue = yBuff
	End Sub

	Friend Sub ReadProperty(ByVal sElementName As String, ByRef sElementValue As E_TYPE_3)
		Dim yBuff As Short = 0
		yBuff = yReadProperty(sElementName, sElementValue)
		sElementValue = yBuff
	End Sub

	Friend Sub ReadProperty(ByVal sElementName As String, ByRef sElementValue As E_LAGFORMAT)
		Dim yBuff As Short = 0
		yBuff = yReadProperty(sElementName, sElementValue)
		sElementValue = yBuff
	End Sub

	Friend Sub ReadProperty(ByVal sElementName As String, ByRef sElementValue As E_TYPE_4)
		Dim yBuff As Short = 0
		yBuff = yReadProperty(sElementName, sElementValue)
		sElementValue = yBuff
	End Sub

	Friend Sub ReadProperty(ByVal sElementName As String, ByRef sElementValue As E_WORKGROUP)
		Dim yBuff As Short = 0
		yBuff = yReadProperty(sElementName, sElementValue)
		sElementValue = yBuff
	End Sub

	Friend Sub ReadProperty(ByVal sElementName As String, ByRef sElementValue As E_ACCRUEAT)
		Dim yBuff As Short = 0
		yBuff = yReadProperty(sElementName, sElementValue)
		sElementValue = yBuff
	End Sub

	Friend Sub ReadProperty(ByVal sElementName As String, ByRef sElementValue As E_STANDARDRATEFORMAT)
		Dim yBuff As Short = 0
		yBuff = yReadProperty(sElementName, sElementValue)
		sElementValue = yBuff
	End Sub

	Friend Sub ReadProperty(ByVal sElementName As String, ByRef sElementValue As E_OVERTIMERATEFORMAT)
		Dim yBuff As Short = 0
		yBuff = yReadProperty(sElementName, sElementValue)
		sElementValue = yBuff
	End Sub

	Friend Sub ReadProperty(ByVal sElementName As String, ByRef sElementValue As E_BOOKINGTYPE)
		Dim yBuff As Short = 0
		yBuff = yReadProperty(sElementName, sElementValue)
		sElementValue = yBuff
	End Sub

	Friend Sub ReadProperty(ByVal sElementName As String, ByRef sElementValue As E_RATETABLE)
		Dim yBuff As Short = 0
		yBuff = yReadProperty(sElementName, sElementValue)
		sElementValue = yBuff
	End Sub

	Friend Sub ReadProperty(ByVal sElementName As String, ByRef sElementValue As E_STANDARDRATEFORMAT_1)
		Dim yBuff As Short = 0
		yBuff = yReadProperty(sElementName, sElementValue)
		sElementValue = yBuff
	End Sub

	Friend Sub ReadProperty(ByVal sElementName As String, ByRef sElementValue As E_COSTRATETABLE)
		Dim yBuff As Short = 0
		yBuff = yReadProperty(sElementName, sElementValue)
		sElementValue = yBuff
	End Sub

	Friend Sub ReadProperty(ByVal sElementName As String, ByRef sElementValue As E_WORKCONTOUR)
		Dim yBuff As Short = 0
		yBuff = yReadProperty(sElementName, sElementValue)
		sElementValue = yBuff
	End Sub

	Friend Sub ReadProperty(ByVal sElementName As String, ByRef sElementValue As E_TYPE_5)
		Dim yBuff As Short = 0
		yBuff = yReadProperty(sElementName, sElementValue)
		sElementValue = yBuff
	End Sub

	Friend Sub ReadProperty(ByVal sElementName As String, ByRef sElementValue As E_UNIT)
		Dim yBuff As Short = 0
		yBuff = yReadProperty(sElementName, sElementValue)
		sElementValue = yBuff
	End Sub
End Class
